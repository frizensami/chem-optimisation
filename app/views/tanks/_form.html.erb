<%= form_for @tank, :html => { :class => "form-horizontal tank" } do |f| %>

  <% if @tank.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@tank.errors.count, "error") %> prohibited this tank from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @tank.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>
  <div class="form-group">
    <%= f.label :capacity_min, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :capacity_min, :class => 'form-control' %>
    </div>
    <%=f.error_span(:capacity_min) %>
  </div>
  <div class="form-group">
    <%= f.label :capacity_max, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :capacity_max, :class => 'form-control' %>
    </div>
    <%=f.error_span(:capacity_max) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tanks_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
